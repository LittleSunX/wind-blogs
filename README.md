# Wind Blogs

一个现代化的技术博客项目，使用 React + TypeScript + Vite 构建，支持 Markdown 格式的文章。

## 技术栈

- **React 18** - 用户界面库
- **TypeScript** - 类型安全的 JavaScript
- **Vite** - 快速的构建工具
- **React Router** - 路由管理
- **React Markdown** - Markdown 渲染
- **React Syntax Highlighter** - 代码高亮

## 功能特性

- 📝 支持 Markdown 格式编写文章
- 🎨 代码语法高亮
- 🏷️ 文章分类和标签系统
- 📱 响应式设计，支持移动端
- ⚡ 基于 Vite 的快速开发体验
- 🔍 SEO 友好

## 开始使用

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:5173 查看博客

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 添加新文章

在 `src/posts/` 目录下创建新的 `.md` 文件即可。文章文件需要包含 frontmatter 元数据：

```markdown
---
title: 文章标题
date: 2025-01-06
excerpt: 文章摘要，会显示在文章列表中
author: 作者名
category: 分类
tags:
  - 标签1
  - 标签2
---

# 文章标题

使用 Markdown 编写你的文章内容...

## 二级标题

内容...
```

### Frontmatter 字段说明

- `title` (必填): 文章标题
- `date` (必填): 发布日期，格式 `YYYY-MM-DD`
- `excerpt` (可选): 文章摘要，如不填写会自动截取正文前150字符
- `author` (可选): 作者名称
- `category` (可选): 文章分类
- `tags` (可选): 标签数组

### 文件名规范

- 文件名就是文章的 URL slug
- 使用小写字母、数字和连字符
- 例如：`my-first-post.md` 对应 URL `/post/my-first-post`

## 目录结构

```
wind_blogs/
├── src/
│   ├── components/       # React 组件
│   │   ├── Layout.tsx
│   │   └── MarkdownRenderer.tsx
│   ├── pages/           # 页面组件
│   │   ├── Home.tsx
│   │   └── Post.tsx
│   ├── posts/           # Markdown 文章目录 ⭐️
│   │   ├── welcome-to-my-blog.md
│   │   ├── understanding-react-hooks.md
│   │   └── ...
│   ├── styles/          # 样式文件
│   │   ├── Layout.css
│   │   ├── Home.css
│   │   └── Post.css
│   ├── utils/           # 工具函数
│   │   └── posts.ts
│   ├── App.tsx
│   ├── main.tsx
│   └── types.ts
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 自定义配置

### 修改主题颜色

在 `src/index.css` 中修改 CSS 变量：

```css
:root {
  --primary-color: #646cff;
  --background-color: #242424;
  /* 其他颜色变量... */
}
```

### 修改网站信息

在 `src/components/Layout.tsx` 中修改网站标题和导航链接。

## 部署

### Vercel

1. 将代码推送到 GitHub
2. 在 Vercel 中导入项目
3. Vercel 会自动检测 Vite 项目并进行部署

### Netlify

1. 将代码推送到 GitHub
2. 在 Netlify 中导入项目
3. 设置构建命令为 `npm run build`
4. 设置发布目录为 `dist`

### GitHub Pages

1. 修改 `vite.config.ts` 添加 base 路径
2. 构建项目：`npm run build`
3. 将 `dist` 目录推送到 `gh-pages` 分支

## 后续改进计划

- [x] 从 Markdown 文件系统读取文章
- [ ] 添加文章搜索功能
- [ ] 添加 RSS 订阅
- [ ] 添加文章评论功能
- [ ] 添加暗色/亮色主题切换
- [ ] 优化 SEO 元标签
- [ ] 添加文章分页
- [ ] 添加阅读时间估算

## 许可证

MIT
